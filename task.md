# 알지오메스 클론코딩 — 진행 체크리스트

> 전체 로드맵 및 순서: [`README.md`](README.md) 참고

---

# 1단계 — 작도 도구 및 엔진 구현

- `[x]` 1. UI 구조 잡기 및 스타일 정의 (HTML & CSS)
    - `[x]` 상단 도구바 (이동, 점, 선분, 원 등 버튼 배치)
    - `[x]` 좌측 대수창 (Sidebar) - 기하 객체 리스트 표시 영역
    - `[x]` 우측 메인 영역 (Canvas 컨테이너)
- `[x]` 2. 좌표 변환 및 그리드 렌더러 구현 (AlgeoRenderer)
    - `[x]` 캔버스 드래그로 화면 이동 (Pan)
    - `[x]` 마우스 휠로 줌인/줌아웃 (Zoom)
    - `[x]` 현재 줌 스케일에 맞는 눈금 격자(Grid) 및 축(Axis) 렌더링
- `[x]` 3. 수학 종속성 엔진 설계 (AlgeoEngine)
    - `[x]` Point, Segment, Circle 등의 기초 객체 모델 선언 (전통 생성자 함수)
    - `[x]` 객체 간의 관계를 가지는 DAG(종속성 그래프) 자료 구조 및 갱신 로직 구현
- `[x]` 4. 이벤트 컨트롤러 구현 및 도구 연동 (AlgeoApp)
    - `[x]` 도구 선택 모드 관리 (점, 선분, 이동 등)
    - `[x]` 마우스 클릭 시 점(Point) 생성
    - `[x]` 점과 점을 연결하는 선분(Segment) 생성
    - `[x]` 원(Circle) 작도 — 중심점과 둘레 점 선택
    - `[x]` 삭제(DELETE) 도구 — 점/선분/원 클릭 삭제 및 종속 객체 연쇄 제거
    - `[x]` 드래그 시 점과 연결된 선분·원이 실시간으로 갱신되며 이동
    - `[x]` 선분/원 작도 중 선택 점 시각적 강조 표시
- `[x]` 5. 대수창(Sidebar) 동적 갱신
    - `[x]` 객체 추가/이동/삭제 시 대수창의 리스트 동적 리프레시 (jQuery 사용)

---

# 2단계 — 대수창 수식 입력 및 파서

- `[x]` 1. 대수창 입력 UI
    - `[x]` 하단 입력창 및 입력 버튼 추가
    - `[x]` Enter 키로 수식 제출
    - `[x]` 오류 메시지 표시 영역
- `[x]` 2. 기초 수식 파서
    - `[x]` `A = (1, 2)` 형태 점 좌표 정의문 파싱
    - `[x]` 동일 이름 점이 있으면 좌표 이동, 없으면 새 점 생성
- `[ ]` 3. 추가 수식 지원
    - `[x]` `y = 2x + 1` 함수 그래프 (2-3)
    - `[x]` `AB` 선분, `⊙(A,B)` 원 등 기하 객체 직접 정의 (2-4)
    - `[x]` 대수창 항목 클릭 시 캔버스 객체 하이라이트 (2-5)

---

# 3단계 — 고급 작도 도구

- `[x]` 직선 (두 점 지나는 무한 직선, 도구바 + Line(A,B) 입력) (3-1)
- `[x]` 중점, 수직이등분선 (3-2)
- `[x]` 평행선, 수직선 (3-3)
- `[ ]` 각도, 호 ← **다음 작업**
- `[ ]` 다각형

---

# 4단계 — 대수창 고도화 (예정)

- `[ ]` 객체 속성 인라인 편집
- `[ ]` Undo / 수식 히스토리
- `[ ]` 슬라이더 변수 연동
- `[ ]` 객체 표시/숨김

---

# 5단계 — UX 및 안정화 (예정)

- `[ ]` 터치 이벤트
- `[ ]` 키보드 단축키
- `[ ]` 다크/라이트 테마
- `[ ]` 파일 저장·불러오기
